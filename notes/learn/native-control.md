# Native control before a custom widget

Published: `source/learn/native-control.html.md` → `/learn/native-control/`

Draft mastery: The learner can replace a clickable `div` with `button` or `a href` and say which one fits.

- [x] Confirm the one-line mastery criterion
- [x] Locator item — `/learn/native-control/pick/` (pool in `data/native_control_pick.yml`)
- [x] Constructed item — `/learn/native-control/fix/` (draws from the bad pool)
- [x] Isomorphic retest item — pick redraw; fix **Another example**
