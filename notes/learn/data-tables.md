# Data tables have headers

Published: `source/learn/data-tables.html.md` → `/learn/data-tables/`

Draft mastery: Given a grid of figures, the learner marks column and row headers (or refuses a layout table).

- [x] Confirm the one-line mastery criterion
- [x] Locator item — `/learn/data-tables/pick/` (pool in `data/data_tables_pick.yml`)
- [x] Constructed item — `/learn/data-tables/fix/` (draws from the bad pool)
- [x] Isomorphic retest item — pick redraw; fix **Another example**
