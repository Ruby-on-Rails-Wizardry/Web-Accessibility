# Do not skip heading levels

Published: `source/learn/heading-order.html.md` → `/learn/heading-order/`

Draft mastery: Given a heading list that skips a level, the learner restores a nested outline without changing the meaning.

- [x] Confirm the one-line mastery criterion
- [x] Locator item — `/learn/heading-order/pick/` (pool in `data/heading_order_pick.yml`; six mixed cards; mark every card that follows the rule)
- [x] Constructed item — `/learn/heading-order/fix/` (draws from the bad pool)
- [x] Isomorphic retest item — pick redraws a different set per visit; fix **Another example** draws a different miss
