# Meaningful sequence

Published: `source/learn/meaningful-sequence.html.md` → `/learn/meaningful-sequence/`

Draft mastery: The learner can point at a CSS-reordered block and restore a DOM order that matches the meaning.

The checker looks for reorder classes (`pull-up`, `visually-first`, `css-order`) and `order` / `flex-direction: *-reverse` in a style attribute.

- [x] Confirm the one-line mastery criterion
- [x] Locator item — `/learn/meaningful-sequence/pick/`
- [x] Constructed item — `/learn/meaningful-sequence/fix/`
- [x] Isomorphic retest item
