# Changing a value does not navigate

Published: `source/learn/no-change-on-input.html.md` → `/learn/no-change-on-input/`

Draft mastery: The learner can replace an onchange navigation with an explicit submit (or an equivalent requested action).

Live specimens keep `location` in the `onchange` attribute, wrapped in `void(...)` so changing the control does not leave the lesson.

- [x] Confirm the one-line mastery criterion
- [x] Locator item — `/learn/no-change-on-input/pick/`
- [x] Constructed item — `/learn/no-change-on-input/fix/`
- [x] Isomorphic retest item
