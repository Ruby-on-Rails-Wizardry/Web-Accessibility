# Testing tools (internal)

Published: `source/docs/evaluation/testing-tools/`

Voice: learner using the site. Host/lab status lives in the per-tool notes in this folder.

Expected heading-order specimen (do not copy onto every tool page):

- `/learn/heading-order/bad/` — `h1` Permits, `h4` Residential, `h4` Commercial
- `/learn/heading-order/good/` — last two are `h2`

Close Edit before scanning so the textarea is not in the DOM.

Deeper DevTools walkthrough on the knowledge tree is still later (`TODO.md`).
